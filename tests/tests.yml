- name: Echo with quotes
  input: "echo salut"

- name: Simple ls
  input: ls

- name: Simple if
  input: |
    if echo ok; then 
      echo foobar
    fi

- name: Medium if
  input: |
    if false; then
      echo "true"
    else
      echo "false"
    fi

- name: Pipe missing right
  input: echo foo |
  checks:
    - stdout
    - has_stderr
